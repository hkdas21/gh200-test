# .github/workflows/cohack-journey.yml
name: 🚀 CoHack Journey

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: 👋 Say Hello
        run: echo "Hello from GH-200 CoHack! Let's build something awesome 🚀"
