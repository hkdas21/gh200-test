name: 🚀 CoHack Journey

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: 👋 Say Hello
        run: echo "Hello CoHack!"

      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📂 List Files
        run: ls -la
